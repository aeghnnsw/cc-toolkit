interface:
  display_name: "Repository Cleanup"
  short_description: "Clean merged worktrees and Git branches"
  default_prompt: "Use $repo-cleanup to clean merged worktrees and branches in this repository."

policy:
  allow_implicit_invocation: false
